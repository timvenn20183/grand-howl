    <% if !@entry.raw.blank? %>
<%= image_tag(@entry.raw.landing.url) %><br/>
    <%= link_to content_tag(:span,'', :class => 'glyphicon glyphicon-trash pull-right', :style => 'color : #333;'), { controller: :entries, action: :remove_image, id: @entry.slug }, method: :delete, data: { confirm: 'Are you sure you wish to delete this image ?', :remote => true } %>
    <% end %>

